name: aish_domain
description: >-
  Pure Dart identity, tenancy and authorization projections for Step 3: User,
  Tenant, Membership, LaundryBrand, Outlet, Role, Permission,
  EffectivePermissions and SessionState. MUST NOT depend on Flutter, and carries
  no Step 4+ business type.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  aish_core: 0.1.0
  meta: 1.18.0

dev_dependencies:
  flutter_lints: 6.0.0
  test: 1.31.0
