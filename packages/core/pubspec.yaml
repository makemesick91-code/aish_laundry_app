name: aish_core
description: >-
  Pure Dart foundation for the Aish Laundry App: Result type, error taxonomy,
  environment configuration, correlation identifiers, clock abstraction and
  application version metadata. MUST NOT depend on Flutter.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  meta: 1.18.0

dev_dependencies:
  flutter_lints: 6.0.0
  test: 1.31.0
