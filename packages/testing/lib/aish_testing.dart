/// Test doubles and helpers for the Step 3 runtime.
///
/// EVERY fixture datum in this package is fictional and recognisably so. There
/// is no real name, no real phone number, no real address, and no copied log
/// line anywhere here. This repository is PUBLIC, so a fixture copied from
/// reality is a permanent disclosure, not a shortcut (Rule 23).
library;

export 'src/api_fixtures.dart';
export 'src/fake_auth_service.dart';
export 'src/fake_clock.dart';
export 'src/fake_tenant_context.dart';
export 'src/in_memory_secure_storage.dart';
export 'src/widget_assertions.dart';
