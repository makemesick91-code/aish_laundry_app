# ci-exact-sha — Step 0

**Status at this commit: PENDING.**

This evidence is produced by a stage that runs after the foundation commit
exists, so it cannot be truthfully populated in the same commit that creates it.
It is deliberately left as an explicit PENDING record rather than pre-filled with
an assumed result.

It is populated in the post-tag evidence-only pull request, which is the
mechanism the Git and release policy defines for adding evidence without moving
the tagged foundation source.

No result is claimed here. Absence of a result is recorded as absence.
